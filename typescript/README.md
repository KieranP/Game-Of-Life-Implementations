# Typescript

## Installation

* `brew install typescript`
* `tsc`

## Usage (Deno)

* `brew install deno`
* `deno run --allow-hrtime --allow-env play.js`

## Usage (Bun)

* `brew install oven-sh/bun/bun`
* `bun run play.js`

## Usage (Node)

* `brew install node`
* `node play.js`
