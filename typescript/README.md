# Typescript

## Installation

* `brew install typescript`

## Usage (Deno)

* `brew install deno`
* `deno run play.ts`

## Usage (Bun)

* `curl -fsSL https://bun.sh/install | bash`
* `bun run play.ts`

## Usage (Node)

* `tsc`
* `brew install node`
* `node play.js`
